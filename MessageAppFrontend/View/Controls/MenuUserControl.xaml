<UserControl x:Class="MessageAppFrontend.View.Controls.MenuUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MessageAppFrontend.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Button Background="Transparent" BorderThickness="0" Command="{Binding OpenUserSettingsCommand}">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Zdjęcie profilowe -->
            <Ellipse Width="50" Height="50" Fill="Gray"/>
            <!-- Nazwa użytkownika -->
            <TextBlock Text="{Binding Username}" Foreground="White" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>
    </Button>
</UserControl>
